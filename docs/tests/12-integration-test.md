# 12-integration-test — 端到端集成测试

**日期：** 2026-07-29

## 测试内容

三个核心功能回归测试：

| # | 功能 | 测试步骤 | 预期 |
|---|------|---------|------|
| 1 | GUI 聊天 | 创建会话 → 输入消息 → 发送 | 消息出现在聊天区，AI 流式回复 |
| 2 | 模型切换 | 选择不同模型 → 发送消息 | 新模型生效 |
| 3 | 会话切换 | 创建多个会话 → 侧栏点击切换 | 聊天区切换为对应历史 |

## 测试步骤

### 功能 1: GUI 聊天
1. 点击 "+" → 输入 "测试" → 选择工作目录 → 创建
2. 输入 "你好，请介绍一下自己" → Enter 发送
3. 观察 AI 回复流式显示
4. 关闭应用 → 重新打开 → 确认会话和历史在侧栏中

### 功能 2: 模型切换
1. 在输入区底部切换模型到 "Haiku 4.5"
2. 发送消息，确认新消息使用新模型
3. 切换回 "Sonnet 4.6"

### 功能 3: 会话切换
1. 创建第二个会话 "Bug修复"
2. 在第二个会话中发送消息
3. 切回第一个会话，确认消息历史完整
4. 删除第二个会话，确认从列表消失

## 预期结果

- 三个核心功能全部正常
- 消息持久化生效（JSONL 文件包含 user + assistant）
- 重启后会话列表和消息历史保留

## 实际结果

- 功能 1：✅ 流式显示正常
- 功能 2：✅ 模型切换正常
- 功能 3：✅ 会话切换+删除正常
- 历史持久化：需要多轮修复（Fix-007/008/009）才完全工作
- 上下文连续性：Fix-013 添加 --continue 标志后正常

## 是否通过

✅ 通过
